pipeline {
    agent any 
    environment{
        IMAGE_NAME = "my-app"
    }

    stages {
        stage("build"){
            steps {
                echo "great ${IMAGE_NAME}"
            }
        }

        stage("test"){
            steps {
                echo "test done"
            }
        }
    }
}
