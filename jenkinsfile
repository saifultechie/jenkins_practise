pipeline {
    agent any 
    environment{
        IMAGE_NAME = "my-app"
    }

    stages {
        stage("build"){
            step{
                echo "great ${IMAGE_NAME}"
            }
        }

        stage("test"){
            step{
                echo "test done"
            }
        }
    }
}